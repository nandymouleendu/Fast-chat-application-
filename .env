# .env work is to hide important data from my database, server link, and keys from users so the app stays secure

PORT=3000
MONGODB_CONNECT=mongodb+srv://nandymouleendu_db_user:Establish%40367@cluster0.svlqcaq.mongodb.net/chatapp?appName=Cluster0
# @ becomes %40 in the password — this is percent-encoding, since @ has special meaning in a URL
# don't use a period (.) in the database name segment, it causes a parsing bug

JWT_SECRET=HelloThereWorld
SECURE=itsDevelopedNow