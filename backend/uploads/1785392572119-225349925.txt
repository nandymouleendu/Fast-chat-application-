npx is node packeage executor which execute any extention without downloading it

create-react-app its a utility means software

in react createroot takes any element from html and inject in js

in react jsx we can technically make our custom tag in </app> and execute it

by react js will not have to be injected in html

react jsx will help us inject js like code into html without any obstacle

The Real Role of React Scriptsreact-scripts is a set of background scripts used by Create React App. It manages the build pipeline, starts your local development server, and compiles your code. It does not handle writing or injecting HTML syntax directly into your JavaScript files.

Babel (which runs behind the scenes inside react-scripts) compiles that JSX into standard JavaScript React.createElement() functions.

React DOM then takes those functions and renders the actual HTML elements into the browser.

rendering of any html file into js is very easy

vite is slightly lighter to code with

in vite same injection of js file into html file by not scrips but direct by main.js(<script type="module" src="/src/main.jsx"></script>)

for normal basicreact the naming is not strict because its a library and you can manipulate it by .js but for vite its a configuration so its name have to be .jsx

in jsx by vite u can technically return/export one element in html but we manipulate it by doing <>...</> and in between these we send many elements except for we could do it by <div><div> 

the function we are exporting that's a component function Chai(){
    return (
        <h3>chai is ready</h3>
    )
}

export default Chai

BEST PRACTICES: always make capitalized name for the function like Chai not chai 
		while importing always write .js/jsx extention in the importing file like from which type of file its importing from

The document.querySelector() method returns the first HTML element inside a webpage that matches a specified CSS selector. If no matching elements are found, it returns null.

/* onClick={addValue()} its button is executed autometicly not onclick */

react controls ui upgradation
		